同步与异步
================

本主题介绍 Python Web 框架中同步与异步的区别、适用场景及 Litestar 的相关实现。

- **同步**：传统阻塞式调用，适合 IO 密集型但并发需求不高的场景。
- **异步**：基于事件循环，支持高并发，适合网络服务、实时数据处理等。

Litestar 支持同步与异步处理器，可根据实际需求选择。

.. note::
    异步代码需使用 `async def`，并确保所有依赖库均为异步兼容。

更多内容请参考 :doc:`/topics/deployment/index`。
